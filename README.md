# pbr
PBR Website

## Setup
Build the containers
```
docker-compose build
```
Launch the containers
```
docker-compose up -d
```
Connect to the `php-apache-pbr` container
```
docker exec -it php-apache-pbr zsh