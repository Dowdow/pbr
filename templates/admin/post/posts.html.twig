{% extends 'admin/base.html.twig' %}

{% block content %}
    <div style="display: flex; flex-direction: row; justify-content: space-between">
        <h2>Posts</h2>
        <a href="{{ path('admin_posts_create') }}" style="align-self: center;"
           class="btn btn-primary active" role="button" aria-pressed="true">Add a post</a>
    </div>
    <div class="row">
        <table class="table">
            <thead>
            <tr>
                <th>ID</th>
                <th>Insta ID</th>
                <th>Sort</th>
                <th>Activated</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
            </thead>
            <tbody>
            {% for post in posts %}
                <tr>
                    <td>{{ post.id }}</td>
                    <td><a href="https://www.instagram.com/p/{{ post.instaId }}/"
                           target="_blank"
                           rel="noopener">{{ post.instaId }}</a></td>
                    <td>
                        <input type="number" class="form-control sort" name="{{ post.id }}" value="{{ post.sort }}">
                    </td>
                    <td>
                        <form method="post">
                            <button type="submit" name="activation_post" value="{{ post.id }}" class="btn btn-primary">
                                {% if post.activated %}
                                    Deactivate
                                {% else %}
                                    Activate
                                {% endif %}
                            </button>
                        </form>
                    </td>
                    <td>
                        <a href="{{ path('admin_posts_edit', {'id': post.id}) }}"
                           class="btn btn-primary" role="button" aria-pressed="true">Edit</a>
                    </td>
                    <td>
                        <form method="post">
                            <button type="submit" name="delete_post" value="{{ post.id }}" class="btn btn-danger">Delete
                            </button>
                        </form>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

{% block js %}
    <script>
        const $sort = document.getElementsByClassName('sort');
        for (let i = 0; i < $sort.length; i++) {
            if ($sort.hasOwnProperty(i)) {
                $sort[i].onchange = (e) => {
                    const id = e.target.name;
                    const value = e.target.value;
                    fetch(`{{ path('admin_posts_sort') }}?id=${id}&sort=${value}`)
                        .then(response => {
                            return response.json();
                        })
                        .then(data => {
                            if (data.message === 'Ok') {
                                e.target.classList.add('border-success');
                            }
                        })
                        .catch(() => {
                            e.target.classList.add('border-danger');
                        });
                }
            }
        }
    </script>
{% endblock %}