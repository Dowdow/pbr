{% extends 'admin/base.html.twig' %}

{% block content %}
	<div class="container p-5 mt-4 mb-4 bg-light rounded-3">
		<div class="container py-5">
			<h1 class="display-5 fw-bold">Dispositif Pain Boudin Record Admin</h1>
			<p class="col-md-8 fs-4">This is the ultimate PBR Admin Interface.</p>
			<a class="btn btn-primary btn-lg" href="{{ path('admin_songs') }}" role="button">Manage Songs</a>
			<a class="btn btn-primary btn-lg" href="{{ path('admin_transitions') }}" role="button">Manage Transitions</a>
		</div>
	</div>
{% endblock %}
