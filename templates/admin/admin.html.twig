{% extends 'admin/base.html.twig' %}

{% block content %}
    <div class="jumbotron">
        <h1 class="display-4">Dispositif Pain Boudin Record Admin</h1>
        <p class="lead">This is the ultimate PBR Admin Interface.</p>
        <hr class="my-4">
        <div class="row justify-content-around flex-wrap">
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ path('admin_songs') }}" role="button">Manage Songs</a>
            </p>
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ path('admin_posts') }}" role="button">Manage Posts</a>
            </p>
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ path('admin_videos') }}" role="button">Manage Videos</a>
            </p>
        </div>
    </div>
{% endblock %}