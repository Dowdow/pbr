{% extends 'admin/base.html.twig' %}

{% block content %}
    <h2><a href="{{ path('admin_songs') }}">Songs</a></h2>
    <h2><a href="{{ path('admin_posts') }}">Posts</a></h2>
    <h2><a href="{{ path('admin_videos') }}">Videos</a></h2>
{% endblock %}